<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.trexin" android:versionCode="1" android:versionName="1.0">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- Developed and tested on Android 4.0.3 (ICE_CREAM_SANDWICH_MR1) -->
    <uses-sdk android:minSdkVersion="15"/>

    <application android:label="Trexin" android:icon="@drawable/circle" android:debuggable="true">
        <activity android:name=".TrexinMobile" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginOffice365" android:label="@string/login" />
    </application>
</manifest>
